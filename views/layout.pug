doctype html
html
  head
    title Asset Management
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css')
    link(rel='stylesheet', href='https://cdn.datatables.net/1.13.1/css/jquery.dataTables.min.css')
    script(defer src='https://code.jquery.com/jquery-3.6.0.min.js')
    script(defer src='https://cdn.datatables.net/1.13.1/js/jquery.dataTables.min.js')
  body
    nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
      .container-fluid
        a.navbar-brand(href='/') Asset Manager
        button.navbar-toggler(type='button', data-bs-toggle='collapse', data-bs-target='#navbarNav')
          span.navbar-toggler-icon
        #navbarNav.collapse.navbar-collapse
          ul.navbar-nav.ms-auto
            li.nav-item
              a.nav-link(href='/employees') Employees
            li.nav-item
              a.nav-link(href='/categories') Categories
            li.nav-item
              a.nav-link(href='/assets') Assets
            li.nav-item
              a.nav-link(href='/stock') Stock View
            li.nav-item
              a.nav-link(href='/transactions/issue') Issue
            li.nav-item
              a.nav-link(href='/transactions/return') Return
            li.nav-item
              a.nav-link(href='/scrap') Scrap
            li.nav-item
              a.nav-link(href='/history') History

    main.container.my-4
      block content

    footer.bg-light.text-center.py-3.mt-auto
      .container
        small.text-muted © #{new Date().getFullYear()} Asset Manager. All rights reserved.

    // Bootstrap Bundle (includes Popper for navbar toggler)
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js')

    // Enable DataTables on all tables with class "dataTable"
    script.
      document.addEventListener('DOMContentLoaded', function () {
        $('table.dataTable').DataTable();
      });
